# Claude.md

Agent skills configuration for the Jetpack Camera App.

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues. Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles are mapped to label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo with shared domain documentation at the root. See `docs/agents/domain.md`.
